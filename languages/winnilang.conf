!+!(CREDITS="Winnigames2024")!+!
public class _info_all()
{
  var name = "WinniLang";
  var language_commands = from winnilang.conf get_classRETURN _commands;
  var creator = "Winnigames2024";

  return(type="name") var.name;
  return(type="creator") var.creator;
  return(type="commands") var.language_commands;
}

return _info_all();
